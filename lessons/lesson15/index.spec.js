import chai from 'chai'

chai.should()

describe('Lesson 16 - Lift into a Pointed Functor with of', () => {
  describe('There is no code nor test file to run so just go ahead and read the readme and move on. ', () => {
    it('love you', () => {
    })
  })
})
