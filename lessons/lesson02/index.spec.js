/* eslint-disable */
import chai, { expect } from 'chai'
import * lesson from './index.js'

chai.should()

describe('Lesson 1', () => {
  describe('bar', () => {
    it('baz', () => {
      expect(1).to.equal(1)
    })
  })
})