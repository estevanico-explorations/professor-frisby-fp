/* eslint-disable */
import chai, { expect } from 'chai'
import index from './index.js'

chai.should()

describe('Lesson 1', () => {
  describe('bar', () => {
    it('baz', () => {
      expect(1).to.equal(1)
    })
  })
})