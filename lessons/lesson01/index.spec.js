/* eslint-disable */
import chai, { expect } from 'chai'
import daggy from 'daggy'
import index from './index.js'

chai.should()

describe('Lesson 1', () => {
  describe('bar', () => {
    it('baz', () => {
      expect(1).to.equal(1)
    })
  })
})