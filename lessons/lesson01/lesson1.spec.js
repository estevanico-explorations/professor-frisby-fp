/* eslint-disable */
import chai, { expect } from 'chai'
import daggy from 'daggy'

chai.should()

describe('foo', () => {
  describe('bar', () => {
    it('baz', () => {
      expect(1).to.equal(1)
    })
  })
})